{
  "name": "microservices-nestjs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/microservices-nestjs/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/microservices-nestjs",
        "main": "apps/microservices-nestjs/src/main.ts",
        "tsConfig": "apps/microservices-nestjs/tsconfig.app.json",
        "assets": [
          "apps/microservices-nestjs/src/assets"
        ],
        "isolatedConfig": true,
        "webpackConfig": "apps/microservices-nestjs/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "microservices-nestjs:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "microservices-nestjs:build:development"
        },
        "production": {
          "buildTarget": "microservices-nestjs:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "apps/microservices-nestjs/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/microservices-nestjs/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
